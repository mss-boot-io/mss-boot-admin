apiVersion: mss.io/v1alpha1
kind: Feature
metadata:
  name: foundation-v1-2-0-release
  displayName: v1.2.0 immutable partial release record
  description: >-
    Records the immutable partial v1.2.0 train from
    c0b4b4d7888a67931951f0b40198d98614e979af: Framework published, while the
    Admin Web publication stopped after tag creation and Root did not publish.
    The remaining goals, requirements, acceptance, and rollout sections preserve
    historical intent and evidence only; they are not current publication authority.
    v1.3.7 is the current stable Distribution.
  owner: release-engineering
  labels:
    domain: release-governance
    stable-at-preparation: v1.1.0
    current-stable-now: v1.3.7
    target-version: v1.2.0
    release-status: immutable-partial
    release-commit: c0b4b4d7888a67931951f0b40198d98614e979af
spec:
  problem: >-
    The active release policy, qualification selector, command catalog, and
    changelogs still target v1.1.0 even though main now contains the V6-only
    Admin application, its browser-session security boundary, new forward
    migrations, and post-v1.1.0 Framework hardening. A release must bind those
    changes to one reviewable v1.2.0 train without treating local evidence or a
    topic-branch commit as publication authority.
  goals:
    - Make v1.2.0 the only active public release target in machine-readable contracts and operator documentation.
    - Qualify root, Framework, and the sole Ant Design 6 frontend from one exact merged-main commit.
    - Prove fresh-install and upgrade migrations, including the explicit API registry synchronization required by menu API binding.
    - Preserve immutable historical tags while making all new publication evidence identify v1.2.0.
    - Keep publication disabled until exact-SHA feature-freeze, pre-framework, and pre-root authority gates pass.
  nonGoals:
    - Repair, move, delete, or republish any v1.1.0 tag or Release.
    - Reintroduce the retired Ant Design 5 application or browser compatibility protocol.
    - Publish a tag, package, image, or GitHub Release from a topic branch.
    - Promote optional ObjectStore or RustFS providers without their independent conformance evidence.
  actors:
    - id: maintainer
      displayName: Foundation maintainer
      description: Freezes implementation and resolves release-blocking defects through reviewed pull requests.
    - id: release-manager
      displayName: Release manager
      description: Selects exact readiness runs and controls immutable component publication.
    - id: admin-operator
      displayName: Admin operator
      description: Applies migrations, synchronizes the API registry, and deploys matching backend/frontend artifacts.
  modules:
    - name: release-governance
      operation: extend
      description: Version policy, qualification scope, exact-SHA evidence, and publication order.
    - name: framework
      operation: extend
      description: Source-compatible Framework hardening published under mss-boot/v1.2.0.
    - name: admin-v6
      operation: extend
      description: V6-only Admin backend, browser contract, migration path, and frontend artifact.
    - name: operations
      operation: extend
      description: Fresh-install, upgrade, API registry synchronization, rollback, and reconciliation guidance.
  requirements:
    - id: target-v120-only
      title: All active release surfaces target v1.2.0
      description: No active policy, command, qualification, or readiness path can accidentally qualify v1.1.0.
      priority: must
      actor: release-manager
      module: release-governance
      rules:
        - The reviewed public version is exactly v1.2.0 for root, Framework, and frontend tag namespaces.
        - Historical Feature contracts and tags remain immutable audit records and are excluded with explicit carry-forward decisions.
        - Project and lock snapshot identity remain their real development values until deterministic snapshot synchronization changes them.
    - id: prove-framework-compatibility
      title: Framework v1.2.0 remains source compatible
      description: The nested Go module publishes its post-v1.1 hardening without an incompatible reusable API removal.
      priority: must
      actor: maintainer
      module: framework
      rules:
        - Delete controls use transactional before/delete boundaries and fixed public error mapping.
        - Password verification uses the existing scrypt representation with constant-time comparison.
        - SecretRef fingerprints remain non-reversible and do not reveal environment locators.
        - GOWORK=off Framework tests pass before the nested-module tag is authorized.
    - id: prove-admin-upgrade
      title: Admin fresh-install and upgrade paths are complete
      description: Schema and seed state support the V6-only application after either a fresh migration or an upgrade.
      priority: must
      actor: admin-operator
      module: operations
      rules:
        - Language, Option, example-Supplier retirement, account reauthentication, and retired-V5 configuration migrations are forward-only and covered by upgrade tests.
        - A fresh or upgraded environment runs `STAGE=local go run . server -a` from admin once to synchronize mounted routes into the API registry.
        - Menu API binding is not considered ready when the API registry is empty, even if backend and frontend health checks pass.
        - Rollback restores a database backup and the preceding qualified V6 backend/frontend pair; it never attempts a schema down-migration in place.
    - id: qualify-v6-product
      title: The sole V6 product passes concentrated qualification
      description: Backend, frontend, delivery, security, and browser evidence all identify the frozen v1.2.0 commit.
      priority: must
      actor: maintainer
      module: admin-v6
      rules:
        - The retired V5 source and browser protocol remain absent.
        - Dependency, lint, unit/integration, Chromium E2E, bundle, and same-origin delivery gates pass.
        - One Codex in-app Browser acceptance proves navigation, RBAC, menu API binding, sessions, settings, and visible failure states on the exact frozen commit.
        - The external Blueprint upgrade rehearsal is bound to the same frozen commit.
    - id: publish-exact-v120
      title: Publish immutable v1.2.0 components from one commit
      description: Every tag, artifact, image, checksum, and Release points to the same approved merged-main commit.
      priority: must
      actor: release-manager
      module: release-governance
      rules:
        - Framework publishes first and resolves from an external GOWORK=off consumer before root publication.
        - The V6 frontend tag and image use web/antd-v6/v1.2.0 and the same frozen commit.
        - Distinct selected pre-framework and pre-root readiness runs grant publication authority.
        - Any implementation change after freeze selects a new commit and invalidates affected evidence.
  constraints:
    - id: merged-main-only
      type: operations
      statement: Qualification and publication require a clean exact commit contained in fresh origin/main and produced by a merged pull request targeting main.
    - id: semver-boundary
      type: compatibility
      statement: v1.2.0 may remove the retired Admin browser compatibility surface documented in the upgrade guide, but it must not remove or incompatibly change a public reusable Framework API.
    - id: one-frontend
      type: compatibility
      statement: web/antd-v6 is the only source, generator target, runtime, image, and frontend release namespace.
    - id: no-secret-evidence
      type: security
      statement: Release reports contain no credentials, database DSNs, tokens, cookies, secret values, or sensitive request bodies.
  acceptance:
    - id: checkpoint-v120-contract
      requirement: target-v120-only
      statement: The new release Feature, policy, qualification selector, workflows, and command catalog agree on v1.2.0.
      level: contract
      phase: checkpoint
      required: true
      evidence:
        - type: command
          value: go run ./cmd/mss spec validate .mss/features/foundation-v1-2-0-release.yaml --format json
        - type: command
          value: python3 -m unittest discover -s tools/release -p 'test_*.py'
        - type: command
          value: python3 tools/release/check_release_policy.py --component root --version v1.2.0 --tag v1.2.0 --intent qualify
        - type: command
          value: python3 tools/release/check_release_policy.py --component framework --version v1.2.0 --tag mss-boot/v1.2.0 --intent qualify
        - type: command
          value: python3 tools/release/check_release_policy.py --component frontend --version v1.2.0 --tag web/antd-v6/v1.2.0 --intent qualify
    - id: feature-freeze-framework
      requirement: prove-framework-compatibility
      statement: The reusable module passes its complete isolated test suite with no workspace replacement.
      level: integration
      phase: feature-freeze
      required: true
      evidence:
        - type: command
          value: cd mss-boot && GOWORK=off go test ./...
    - id: feature-freeze-backend-upgrade
      requirement: prove-admin-upgrade
      statement: Root tooling and the complete Admin backend, including migrations and API registry synchronization paths, pass on the frozen commit.
      level: integration
      phase: feature-freeze
      required: true
      evidence:
        - type: command
          value: go test ./...
        - type: command
          value: cd admin && go test ./...
        - type: manual
          value: On isolated fresh and upgraded SQLite, MySQL, and PostgreSQL databases, run migrations, then run `STAGE=local go run . server -a` from admin and confirm the menu API-binding selector is non-empty.
        - type: report
          value: Exact-SHA fresh-install and upgrade reports for SQLite, MySQL, and PostgreSQL with no skipped required migration assertion.
    - id: feature-freeze-v6
      requirement: qualify-v6-product
      statement: The sole frontend passes the concentrated dependency, behavior, browser, bundle, and delivery gates.
      level: e2e
      phase: feature-freeze
      required: true
      evidence:
        - type: command
          value: cd web/antd-v6 && corepack pnpm@10.34.5 deps:check
        - type: command
          value: cd web/antd-v6 && corepack pnpm@10.34.5 audit:release
        - type: command
          value: cd web/antd-v6 && corepack pnpm@10.34.5 lint
        - type: command
          value: cd web/antd-v6 && corepack pnpm@10.34.5 test:ci
        - type: command
          value: cd web/antd-v6 && corepack pnpm@10.34.5 test:e2e
        - type: command
          value: cd web/antd-v6 && corepack pnpm@10.34.5 build:release
        - type: command
          value: cd web/antd-v6 && corepack pnpm@10.34.5 delivery:smoke
    - id: feature-freeze-docs
      requirement: target-v120-only
      statement: Release, upgrade, API registry, compatibility, and rollback guidance build successfully.
      level: contract
      phase: feature-freeze
      required: true
      evidence:
        - type: command
          value: corepack pnpm@9.15.9 --dir docs build
    - id: feature-freeze-sha-bound-evidence
      requirement: qualify-v6-product
      statement: Browser acceptance and the external Blueprint rehearsal are both bound to the exact frozen commit.
      level: manual
      phase: feature-freeze
      required: true
      evidence:
        - type: report
          value: .mss/reports/release-qualification-decision.json
        - type: manual
          value: Supply concrete in-app Browser and Blueprint evidence references whose full commits equal the frozen workflow SHA.
    - id: pre-framework-authority
      requirement: publish-exact-v120
      statement: A selected pre-framework readiness run grants exact authority before Framework or frontend publication.
      level: manual
      phase: pre-framework
      required: true
      evidence:
        - type: command
          value: bash tools/release/verify_readiness_run.sh --repository <owner/repository> --run-id <run-id> --target-version v1.2.0 --commit <full-sha> --phase pre-framework --policy .mss/release-policy.yaml
        - type: report
          value: release-readiness-attestation-<run-id> artifact retained from the selected successful run
    - id: pre-root-authority
      requirement: publish-exact-v120
      statement: Framework v1.2.0 resolves externally and a separately selected pre-root run authorizes root publication.
      level: integration
      phase: pre-root
      required: true
      evidence:
        - type: command
          value: bash tools/release/verify_readiness_run.sh --repository <owner/repository> --run-id <run-id> --target-version v1.2.0 --commit <full-sha> --phase pre-root --policy .mss/release-policy.yaml
        - type: manual
          value: Resolve github.com/mss-boot-io/mss-boot-admin/mss-boot@v1.2.0 from a clean external module with GOWORK=off and record the checksum.
    - id: post-publication-truth
      requirement: publish-exact-v120
      statement: Public tags, Releases, archives, images, checksums, installation smoke, migration state, and rollback evidence reconcile to one commit.
      level: integration
      phase: post-publication
      required: true
      evidence:
        - type: report
          value: Final v1.2.0 reconciliation comment on the release evidence issue
        - type: manual
          value: Close the evidence issue only after root, Framework, frontend, container, docs, install, API registry, and recovery facts agree.
  risks:
    - id: incomplete-api-registry
      description: A healthy fresh installation can still show no bindable APIs when route metadata was not synchronized.
      severity: high
      mitigation: Make the one-shot server -a command an explicit quick-start and upgrade step and verify a non-empty selector before release acceptance.
    - id: schema-rollback
      description: Retired V5 and account-security migrations cannot be safely reversed by redeploying old binaries against the mutated database.
      severity: critical
      mitigation: Require a tested backup/restore point and roll back the database together with a preceding qualified V6 artifact pair.
    - id: prerelease-ui-dependency
      description: ProComponents v3 prerelease behavior may change despite an exact package pin.
      severity: medium
      mitigation: Keep the exact pin, dependency gate, focused tests, and release bundle checks until a stable v3 passes the same suite.
    - id: stale-candidate-evidence
      description: Evidence from a topic branch or earlier commit can be mistaken for publication authority.
      severity: critical
      mitigation: Bind every readiness, browser, Blueprint, tag, and artifact record to the selected full merged-main SHA and invalidate evidence after any change.
  validation:
    changed: go run ./cmd/mss verify --changed
    all: python3 tools/release/release_phase_evidence.py run --target-version v1.2.0 --commit <full-sha> --phase feature-freeze --output .mss/reports/release-phase-command-evidence.json
    custom:
      - python3 -m unittest discover -s tools/release -p 'test_*.py'
      - go run ./cmd/mss spec validate .mss/features/foundation-v1-2-0-release.yaml --format json
  rollout:
    strategy: phased
    migration: >-
      Merge preparation through a pull request, select the resulting clean exact
      main commit, run feature-freeze evidence, obtain pre-framework authority,
      publish Framework and verify external GOWORK=off resolution, publish the
      matching V6 frontend and image, obtain pre-root authority, publish root,
      then reconcile installation, migrations, API registry, and recovery.
    rollback: >-
      Stop publication before the next component when a gate fails. After any
      immutable component is public, do not move its tag; forward-repair with a
      higher version. For a deployed Admin failure, restore the pre-migration
      database backup and redeploy the preceding qualified V6 backend/frontend pair.
