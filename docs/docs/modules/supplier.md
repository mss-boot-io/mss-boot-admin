---
title: Suppliers
order: 100
nav:
  title: Business modules
  order: 5
---

<!-- Code generated by mss module template 1.3.0-docs.2 from .mss/modules/example-supplier.yaml. DO NOT EDIT. -->

# Suppliers / 供应商管理

管理供应商基础信息、信用等级、联系人和启用状态。

This document is generated deterministically from [`.mss/modules/example-supplier.yaml`](../../../.mss/modules/example-supplier.yaml). Change the source specification or generator templates instead of editing this file.

## Contract

| Item | Value |
| --- | --- |
| Module | `supplier` |
| Go entity | `Supplier` |
| Database table | `biz_suppliers` |
| Entity migration | `20260810160000` |
| Authorization migration | `20260821093000` |
| API base | `/admin/api/suppliers` |
| Frontend route | `/suppliers` |
| Menu icon | `shop` |
| Parent menu | `/procurement` |
| Parent menu icon | `shop` |
| Frontend target | `antd-v6` |
| Ownership | role-only; no row ownership fallback |

Both migrations are additive and must be applied by the Admin migration command before production routes are mounted. Authorization remains enforced by the Admin database-backed Casbin policy; frontend visibility is only a user-experience aid.

Set `spec.menu.icon` for the module leaf and `spec.menu.parentIcon` for its parent directory. The frontend resolves icon keys through its safe registry and falls back without executing arbitrary component names.

## Fields

| Field | Display name | Type | Required | Searchable | Filterable |
| --- | --- | --- | --- | --- | --- |
| `code` | 供应商编码 | `string` | yes | yes | no |
| `name` | 供应商名称 | `string` | yes | yes | no |
| `country` | 国家或地区 | `string` | no | no | yes |
| `contactName` | 联系人 | `string` | yes | yes | no |
| `contactEmail` | 联系邮箱 | `string` | no | no | no |
| `creditLevel` | 信用等级 | `enum` | yes | no | yes |
| `enabled` | 启用状态 | `bool` | yes | no | yes |

## API and permissions

| Operation | Method | Path | Permission | Default roles |
| --- | --- | --- | --- | --- |
| `create` | `POST` | `/admin/api/suppliers` | `supplier:create` | `admin`, `procurement` |
| `delete` | `DELETE` | `/admin/api/suppliers/{id}` | `supplier:delete` | `admin` |
| `export` | `GET` | `/admin/api/suppliers/export` | `supplier:export` | `admin`, `finance`, `procurement` |
| `get` | `GET` | `/admin/api/suppliers/{id}` | `supplier:read` | `admin`, `finance`, `procurement` |
| `list` | `GET` | `/admin/api/suppliers` | `supplier:list` | `admin`, `finance`, `procurement` |
| `update` | `PUT` | `/admin/api/suppliers/{id}` | `supplier:update` | `admin`, `procurement` |

## Generated UI and browser acceptance

Each selected frontend target receives an isolated generated page with the declared loading, empty, error, forbidden, validation, conflict, and destructive-confirmation states. Browser entries are target-specific:

- Ant Design 6: `web/antd-v6/e2e/generated/supplier.spec.ts`.

Each generated flow covers the declared surfaces:

- create through the generated form;
- open and verify the detail drawer;
- edit one mutable field and observe the persisted row;
- export and verify the downloaded filename;
- confirm deletion and verify the row disappears.

The checked-in test is an executable contract, not proof that a browser run occurred. Record the actual Playwright or in-app-browser result separately with its server state and commit identity.

## Generate and validate

From the repository root:

```shell
go run ./cmd/mss spec validate .mss/modules/example-supplier.yaml --format json
go run ./cmd/mss module generate .mss/modules/example-supplier.yaml --frontend-target antd-v6 --write
go run ./cmd/mss module generate .mss/modules/example-supplier.yaml --frontend-target antd-v6 --check
cd admin && GOWORK=off go test ./modules/supplier/...
cd web/antd-v6 && pnpm exec playwright test e2e/generated/supplier.spec.ts --project=chromium-desktop
```

`MSS_E2E_API_URL`, `MSS_E2E_USERNAME`, and `MSS_E2E_PASSWORD` may be set for a local non-production Admin instance. V6 additionally accepts `MSS_V6_BASE_URL`. Browser execution requires both Admin and the selected frontend development server to be ready.
