---
title: 全量测试用例
order: 28
nav:
  order: 1
  title: admin
description: mss-boot-admin 完整功能测试用例，覆盖所有核心模块和边界场景
keywords: [admin test cases integration e2e]
---

## 概述

本文档定义 `mss-boot-admin` 的全量测试用例，覆盖：

- 认证授权
- 用户管理
- 角色权限
- 组织架构
- 菜单配置
- 日志管理
- 系统监控
- 存储管理
- 任务管理
- 告警通知

> **测试时间**：2026-04-03
> **测试版本**：产品打磨三轮完成后
> **测试类型**：集成测试 + 功能测试

## 测试环境

### 后端
- 服务地址：`http://localhost:8080`
- 数据库：SQLite（默认）/ MySQL / PostgreSQL
- Redis：可选（WebSocket 集群需要）

### 前端
- 访问地址：`http://localhost:8000`
- 浏览器：Chrome / Firefox / Safari

### 测试账号
- 管理员：`admin` / `123456`（默认密码）
- 普通用户：测试时创建

---

## 模块 1：认证与授权

### 1.1 登录功能

#### TC-AUTH-001：账号密码登录 - 成功
**前置条件**：用户已注册
**测试步骤**：
1. 访问登录页面 `/user/login`
2. 输入正确用户名 `admin`
3. 输入正确密码 `123456`（默认密码）
4. 点击登录按钮
**预期结果**：
- 登录成功
- 跳转到首页 `/`
- 顶部显示用户信息
- localStorage 中存在 token

#### TC-AUTH-002：账号密码登录 - 失败（错误密码）
**前置条件**：用户已注册
**测试步骤**：
1. 访问登录页面
2. 输入正确用户名
3. 输入错误密码
4. 点击登录按钮
**预期结果**：
- 登录失败
- 显示错误提示"登录失败，请重试！"
- 停留在登录页面

#### TC-AUTH-003：账号密码登录 - 失败（用户不存在）
**测试步骤**：
1. 输入不存在的用户名
2. 输入任意密码
3. 点击登录
**预期结果**：
- 登录失败
- 显示错误提示

#### TC-AUTH-004：自动登录
**前置条件**：上次登录时勾选了"自动登录"
**测试步骤**：
1. 关闭浏览器
2. 重新打开浏览器访问系统
**预期结果**：
- 自动登录成功
- 不需要重新输入密码

#### TC-AUTH-005：GitHub OAuth 登录
**前置条件**：系统配置了 GitHub OAuth
**测试步骤**：
1. 点击 GitHub 图标
2. 在弹出窗口中授权
3. 回调成功
**预期结果**：
- 登录成功
- 跳转到首页

#### TC-AUTH-006：飞书 OAuth 登录
**前置条件**：系统配置了飞书 OAuth
**测试步骤**：
1. 点击飞书图标
2. 在弹出窗口中授权
3. 回调成功
**预期结果**：
- 登录成功

### 1.2 登出功能

#### TC-AUTH-007：登出
**前置条件**：用户已登录
**测试步骤**：
1. 点击用户头像
2. 点击"退出登录"
**预期结果**：
- 清除 localStorage 中的 token
- 跳转到登录页面

### 1.3 权限验证

#### TC-AUTH-008：未登录访问受保护页面
**测试步骤**：
1. 未登录状态下直接访问 `/users`
**预期结果**：
- 跳转到登录页面
- URL 带有 `redirect` 参数

#### TC-AUTH-009：登录后跳转回原页面
**前置条件**：TC-AUTH-008 的状态
**测试步骤**：
1. 在登录页面完成登录
**预期结果**：
- 登录成功后跳转回 `/users`

#### TC-AUTH-010：无权限访问
**前置条件**：普通用户，无用户管理权限
**测试步骤**：
1. 登录普通用户
2. 访问 `/users`
**预期结果**：
- 显示"无权限"提示
- 或隐藏该菜单项

---

## 模块 2：用户管理

### 2.1 用户列表

#### TC-USER-001：查看用户列表
**前置条件**：有用户管理权限
**测试步骤**：
1. 访问 `/users`
**预期结果**：
- 显示用户列表表格
- 表格包含：用户名、邮箱、手机、部门、岗位、状态、操作
- 支持分页

#### TC-USER-002：搜索用户
**测试步骤**：
1. 在搜索框输入用户名
2. 点击搜索
**预期结果**：
- 列表只显示匹配的用户

#### TC-USER-003：按状态筛选
**测试步骤**：
1. 选择状态筛选器
2. 选择"启用"或"禁用"
**预期结果**：
- 列表只显示对应状态的用户

### 2.2 创建用户

#### TC-USER-004：创建用户 - 成功
**测试步骤**：
1. 点击"新建"按钮
2. 填写用户信息：
   - 用户名：`testuser001`
   - 邮箱：`test@example.com`
   - 密码：`Test1234`
   - 部门：选择一个部门
   - 岗位：选择一个岗位
   - 角色：选择一个角色
3. 点击"确定"
**预期结果**：
- 创建成功，显示成功提示
- 列表中出现新用户
- 跳转到 `/users`（不是 `/user`）

#### TC-USER-005：创建用户 - 失败（用户名重复）
**测试步骤**：
1. 创建已存在的用户名
**预期结果**：
- 创建失败
- 显示"用户名已存在"提示

#### TC-USER-006：创建用户 - 失败（必填项为空）
**测试步骤**：
1. 不填写用户名
2. 点击"确定"
**预期结果**：
- 显示"请输入用户名"验证提示

#### TC-USER-007：创建用户 - 失败（邮箱格式错误）
**测试步骤**：
1. 输入错误格式的邮箱：`invalid-email`
**预期结果**：
- 显示"邮箱格式错误"验证提示

### 2.3 编辑用户

#### TC-USER-008：编辑用户 - 成功
**前置条件**：存在测试用户
**测试步骤**：
1. 点击用户的"编辑"按钮
2. 修改邮箱为 `newemail@example.com`
3. 点击"确定"
**预期结果**：
- 编辑成功
- 列表中邮箱已更新
- 跳转到 `/users`

#### TC-USER-009：编辑用户 - 修改密码
**测试步骤**：
1. 在编辑页面修改密码
**预期结果**：
- 密码修改成功
- 下次登录需使用新密码

### 2.4 删除用户

#### TC-USER-010：删除用户 - 成功
**前置条件**：存在测试用户
**测试步骤**：
1. 点击用户的"删除"按钮
2. 确认删除
**预期结果**：
- 删除成功
- 列表中该用户消失

#### TC-USER-011：删除用户 - 失败（删除自己）
**测试步骤**：
1. 尝试删除当前登录用户
**预期结果**：
- 删除失败或按钮禁用

### 2.5 用户详情

#### TC-USER-012：查看用户详情
**测试步骤**：
1. 点击用户行的任意位置（或详情按钮）
**预期结果**：
- 右侧弹出详情抽屉
- 显示用户完整信息

### 2.6 重置密码

#### TC-USER-013：管理员重置用户密码
**前置条件**：存在测试用户
**测试步骤**：
1. 点击用户的"重置密码"按钮
2. 输入新密码：`NewPass123`
3. 确认密码：`NewPass123`
4. 点击"确定"
**预期结果**：
- 重置成功
- 跳转到 `/users`（不是 `/user`）

#### TC-USER-014：重置密码 - 失败（密码不一致）
**测试步骤**：
1. 输入新密码：`NewPass123`
2. 确认密码：`DifferentPass`
3. 点击"确定"
**预期结果**：
- 显示"两次密码不一致"错误

---

## 模块 3：角色管理

### 3.1 角色列表

#### TC-ROLE-001：查看角色列表
**测试步骤**：
1. 访问 `/role`
**预期结果**：
- 显示角色列表
- 包含：角色名、标识、状态、描述、操作

### 3.2 创建角色

#### TC-ROLE-002：创建角色 - 成功
**测试步骤**：
1. 点击"新建"
2. 填写：
   - 角色名：`测试角色`
   - 标识：`test_role`
   - 描述：`用于测试`
3. 点击"确定"
**预期结果**：
- 创建成功
- 列表中出现新角色

### 3.3 编辑角色

#### TC-ROLE-003：编辑角色权限
**测试步骤**：
1. 点击角色的"编辑"按钮
2. 修改权限配置
3. 点击"确定"
**预期结果**：
- 权限更新成功
- 该角色的用户权限立即生效

### 3.4 删除角色

#### TC-ROLE-004：删除角色
**前置条件**：角色未被用户使用
**测试步骤**：
1. 点击"删除"
2. 确认删除
**预期结果**：
- 删除成功

#### TC-ROLE-005：删除角色 - 失败（角色正在使用）
**前置条件**：角色已被用户使用
**测试步骤**：
1. 尝试删除角色
**预期结果**：
- 删除失败
- 提示"角色正在使用，无法删除"

---

## 模块 4：部门管理

### 4.1 部门树

#### TC-DEPT-001：查看部门树
**测试步骤**：
1. 访问 `/departments`
**预期结果**：
- 显示部门树形结构
- 支持展开/收起

### 4.2 创建部门

#### TC-DEPT-002：创建顶级部门
**测试步骤**：
1. 点击"新建"
2. 填写：
   - 部门名：`测试部门`
   - 负责人：选择用户
   - 排序：`1`
3. 点击"确定"
**预期结果**：
- 创建成功
- 部门树中出现新部门
- 跳转到 `/departments`（不是 `/department`）

#### TC-DEPT-003：创建子部门
**测试步骤**：
1. 点击部门的"添加子部门"
2. 填写信息
3. 点击"确定"
**预期结果**：
- 子部门创建成功
- 显示在父部门下

### 4.3 编辑部门

#### TC-DEPT-004：编辑部门
**测试步骤**：
1. 点击部门的"编辑"
2. 修改部门名
3. 点击"确定"
**预期结果**：
- 编辑成功
- 跳转到 `/departments`

### 4.4 删除部门

#### TC-DEPT-005：删除部门 - 成功
**前置条件**：部门下无用户和子部门
**测试步骤**：
1. 点击"删除"
2. 确认删除
**预期结果**：
- 删除成功

#### TC-DEPT-006：删除部门 - 失败（有子部门）
**测试步骤**：
1. 尝试删除有子部门的部门
**预期结果**：
- 删除失败
- 提示"请先删除子部门"

---

## 模块 5：岗位管理

### 5.1 岗位列表

#### TC-POST-001：查看岗位列表
**测试步骤**：
1. 访问 `/posts`
**预期结果**：
- 显示岗位列表
- 包含：岗位名、标识、排序、状态

### 5.2 创建岗位

#### TC-POST-002：创建岗位
**测试步骤**：
1. 点击"新建"
2. 填写：
   - 岗位名：`测试岗位`
   - 标识：`test_post`
3. 点击"确定"
**预期结果**：
- 创建成功
- 跳转到 `/posts`（不是 `/post`）

---

## 模块 6：菜单管理

### 6.1 菜单树

#### TC-MENU-001：查看菜单树
**测试步骤**：
1. 访问 `/menu`
**预期结果**：
- 显示菜单树形结构
- 支持展开/收起

### 6.2 创建菜单

#### TC-MENU-002：创建菜单项
**测试步骤**：
1. 点击"新建"
2. 填写：
   - 菜单名：`测试菜单`
   - 类型：菜单
   - 路径：`/test`
   - 图标：选择图标
3. 点击"确定"
**预期结果**：
- 创建成功
- 菜单树中出现新菜单

### 6.3 绑定 API

#### TC-MENU-003：菜单绑定 API
**前置条件**：存在菜单项
**测试步骤**：
1. 点击菜单的"绑定API"
2. 输入 API 路径（格式：`方法---路径`，如 `GET---/admin/api/users`）
3. 点击"确定"
**预期结果**：
- 绑定成功
- API 显示在菜单下
- Casbin 规则已创建

#### TC-MENU-004：菜单绑定 API - 失败（非法格式）
**测试步骤**：
1. 输入非法格式：`invalid-format`
**预期结果**：
- 提示错误或拒绝绑定

---

## 模块 7：日志管理

### 7.1 登录日志

#### TC-LOG-001：查看登录日志
**测试步骤**：
1. 访问 `/log`
2. 点击"登录日志"标签
**预期结果**：
- 显示登录日志列表
- 包含：用户名、IP、地点、状态、登录时间
- 日志已记录（包含之前的测试登录）

#### TC-LOG-002：搜索登录日志
**测试步骤**：
1. 输入用户名搜索
2. 选择日期范围
**预期结果**：
- 显示匹配的日志

### 7.2 审计日志

#### TC-LOG-003：查看审计日志
**测试步骤**：
1. 点击"审计日志"标签
**预期结果**：
- 显示操作日志
- 包含：用户、操作类型、资源、IP、状态、时长、时间

#### TC-LOG-004：审计日志记录
**前置条件**：执行一些操作
**测试步骤**：
1. 创建一个用户
2. 查看审计日志
**预期结果**：
- 日志中出现"创建用户"操作记录
- 包含请求和响应信息

### 7.3 运行时日志

#### TC-LOG-005：查看运行时日志
**测试步骤**：
1. 点击"运行时日志"标签
**预期结果**：
- 显示后端日志列表
- 支持按级别筛选（INFO/WARN/ERROR/DEBUG）
- 支持关键词搜索

#### TC-LOG-006：导出日志
**测试步骤**：
1. 点击"导出"按钮
**预期结果**：
- 下载日志文件

---

## 模块 8：系统监控

### 8.1 监控页面

#### TC-MONITOR-001：查看监控数据
**测试步骤**：
1. 访问 `/welcome` 或 `/monitor`
**预期结果**：
- 显示 CPU 使用率
- 显示内存使用率
- 显示磁盘使用率
- 显示趋势图表
- 数据每 30 秒自动刷新

#### TC-MONITOR-002：监控数据错误处理
**前置条件**：后端监控接口返回错误
**测试步骤**：
1. 访问监控页面
**预期结果**：
- 显示错误提示
- 有重试机制

---

## 模块 9：存储管理

### 9.1 文件上传

#### TC-STORAGE-001：上传文件 - 成功
**测试步骤**：
1. 访问用户设置或任意支持上传的页面
2. 选择一个图片文件（< 10MB）
3. 上传
**预期结果**：
- 上传成功
- 返回文件 URL
- URL 格式为 `/public/{userID}/{filename}`

#### TC-STORAGE-002：上传文件 - 失败（文件过大）
**测试步骤**：
1. 选择超大文件（> 配置的最大值）
2. 上传
**预期结果**：
- 上传失败
- 提示"文件大小超过限制"

#### TC-STORAGE-003：上传文件 - 失败（非法类型）
**测试步骤**：
1. 选择不允许的文件类型
2. 上传
**预期结果**：
- 上传失败
- 提示"不支持的文件类型"

### 9.2 存储配置

#### TC-STORAGE-004：配置存储限制
**测试步骤**：
1. 访问 `/app-config?key=storage`
2. 设置：
   - 最大文件大小：`10` MB
   - 允许的文件类型：`.jpg,.png,.pdf`
3. 保存
**预期结果**：
- 配置保存成功
- 上传时按新配置验证

---

## 模块 10：任务管理

### 10.1 任务列表

#### TC-TASK-001：查看任务列表
**测试步骤**：
1. 访问 `/task`
**预期结果**：
- 显示任务列表
- 包含：任务名、Cron 表达式、状态、上次执行时间

### 10.2 创建任务

#### TC-TASK-002：创建定时任务
**测试步骤**：
1. 点击"新建"
2. 填写：
   - 任务名：`测试任务`
   - Cron：`*/5 * * * *`（每 5 分钟）
   - 类型：选择任务类型
3. 点击"确定"
**预期结果**：
- 创建成功
- 任务开始按计划执行

### 10.3 执行任务

#### TC-TASK-003：立即执行任务
**测试步骤**：
1. 点击任务的"立即执行"
**预期结果**：
- 任务执行成功
- 显示执行结果

---

## 模块 11：告警管理

### 11.1 告警规则

#### TC-ALERT-001：创建告警规则
**测试步骤**：
1. 访问告警规则页面
2. 点击"新建"
3. 填写：
   - 规则名：`CPU 高负载告警`
   - 指标：`cpu`
   - 操作符：`>`
   - 阈值：`80`
   - 持续时间：`5m`
4. 点击"确定"
**预期结果**：
- 规则创建成功
- 规则状态为"已启用"

#### TC-ALERT-002：触发告警
**前置条件**：CPU 使用率 > 80% 持续 5 分钟
**测试步骤**：
1. 等待告警触发
2. 查看告警历史
**预期结果**：
- 告警历史中出现记录
- 通知已发送（根据配置）

### 11.2 通知渠道

#### TC-NOTIFY-001：配置邮件通知
**测试步骤**：
1. 访问 `/app-config?key=email`
2. 配置 SMTP 信息
3. 保存
**预期结果**：
- 配置保存成功

---

## 模块 12：语言管理

### 12.1 语言列表

#### TC-LANG-001：查看语言列表
**测试步骤**：
1. 访问 `/language`
**预期结果**：
- 显示支持的语言列表
- 包含：语言名、语言代码、状态

### 12.2 语言切换

#### TC-LANG-002：切换语言
**测试步骤**：
1. 点击右上角语言切换器
2. 选择不同语言
**预期结果**：
- 界面语言切换成功
- 刷新后保持选择

---

## 模块 13：系统配置

### 13.1 基础配置

#### TC-CONFIG-001：修改网站名称
**测试步骤**：
1. 访问 `/app-config?key=base`
2. 修改网站名称
3. 保存
**预期结果**：
- 配置保存成功
- 页面标题更新

### 13.2 安全配置

#### TC-CONFIG-002：配置登录方式
**测试步骤**：
1. 访问 `/app-config?key=security`
2. 启用/禁用 GitHub 登录
3. 保存
**预期结果**：
- 登录页面显示/隐藏 GitHub 图标

---

## 模块 14：边界测试

### 14.1 性能边界

#### TC-EDGE-001：大量数据加载
**测试步骤**：
1. 创建 1000+ 用户
2. 访问用户列表
**预期结果**：
- 页面正常加载
- 分页正常工作

#### TC-EDGE-002：并发登录
**测试步骤**：
1. 同一账号在多个浏览器登录
**预期结果**：
- 根据配置，允许或拒绝后续登录

### 14.2 数据边界

#### TC-EDGE-003：超长输入
**测试步骤**：
1. 创建用户，用户名输入 1000 个字符
**预期结果**：
- 前端验证拦截
- 或后端返回错误

#### TC-EDGE-004：特殊字符
**测试步骤**：
1. 创建用户，用户名包含特殊字符：`<script>alert(1)</script>`
**预期结果**：
- 正确转义，无 XSS 攻击

#### TC-EDGE-005：SQL 注入
**测试步骤**：
1. 登录时用户名输入：`admin' OR '1'='1`
**预期结果**：
- 登录失败
- 无 SQL 注入漏洞

---

## 测试执行清单

### 第一阶段：核心流程
- [ ] TC-AUTH-001：账号密码登录成功
- [ ] TC-AUTH-007：登出
- [ ] TC-USER-004：创建用户
- [ ] TC-USER-008：编辑用户
- [ ] TC-USER-010：删除用户

### 第二阶段：权限与组织
- [ ] TC-ROLE-002：创建角色
- [ ] TC-ROLE-003：编辑角色权限
- [ ] TC-DEPT-002：创建部门
- [ ] TC-POST-002：创建岗位

### 第三阶段：菜单与 API
- [ ] TC-MENU-002：创建菜单
- [ ] TC-MENU-003：绑定 API

### 第四阶段：日志与监控
- [ ] TC-LOG-001：查看登录日志
- [ ] TC-LOG-003：查看审计日志
- [ ] TC-LOG-005：查看运行时日志
- [ ] TC-MONITOR-001：查看监控数据

### 第五阶段：存储与任务
- [ ] TC-STORAGE-001：上传文件
- [ ] TC-TASK-002：创建定时任务

### 第六阶段：边界测试
- [ ] TC-EDGE-001：大量数据加载
- [ ] TC-EDGE-004：特殊字符输入
- [ ] TC-EDGE-005：SQL 注入测试

---

## 缺陷等级定义

### 严重 (Critical)
- 系统崩溃
- 数据丢失
- 安全漏洞

### 高 (High)
- 功能无法使用
- 数据错误
- 性能严重下降

### 中 (Medium)
- 功能部分不可用
- 界面显示错误
- 体验问题

### 低 (Low)
- 文案错误
- 界面美化问题
- 建议

---

## 测试报告模板

### 执行摘要
- 测试时间：YYYY-MM-DD HH:mm
- 测试人员：
- 测试环境：

### 测试统计
- 总用例数：XX
- 通过数：XX
- 失败数：XX
- 阻塞数：XX
- 通过率：XX%

### 缺陷统计
- 严重：XX
- 高：XX
- 中：XX
- 低：XX

### 主要问题
1. 问题描述
2. 重现步骤
3. 预期结果
4. 实际结果
5. 截图/日志

### 建议
- 改进建议
- 遗留问题

---

## 推荐阅读

- [产品打磨与工程治理方案](/admin/product-polish-governance-plan)
- [首轮整改清单](/admin/product-polish-remediation-round-1)
- [集成测试指南](/admin/integration-test-guide)
- [发布验证清单](/admin/release-verification-checklist)