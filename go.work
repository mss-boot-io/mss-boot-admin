go 1.26.0

use (
	.
	./mss-boot
)

// Temporary bridge for the first nested-module release. Remove this line after
// the mss-boot/v0.8.0 tag is published and the root go.sum is refreshed.
replace github.com/mss-boot-io/mss-boot-admin/mss-boot v0.8.0 => ./mss-boot
