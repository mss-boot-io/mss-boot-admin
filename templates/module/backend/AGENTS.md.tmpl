<!-- Code generated by mss module template {{ .InstructionsTemplateRevision }} from {{ .SourceSpec }}. DO NOT EDIT. -->

# {{ .DisplayName }} module instructions

## Scope

This file applies to `admin/modules/{{ .ModuleName }}/` and inherits `admin/AGENTS.md` and the repository-root `AGENTS.md`.

## Source contract

- Canonical specification: `module.yaml`
- Module name: `{{ .ModuleName }}`
- API resource: `/admin/api/{{ .APIResource }}`
- Permission prefix: `{{ .ModuleName }}:`

## Generated and custom code

Files with a `Code generated by mss` header are derived from `module.yaml`. Change the specification or generator templates instead of editing generated output manually.

Put non-template business rules in explicitly custom files such as `service_custom.go`, `hooks_custom.go`, or focused test files without a generated header.

## Required validation

```shell
go run ./cmd/mss spec validate {{ .SourceSpec }}
{{- if .HasFrontendV5 }}
go run ./cmd/mss module generate {{ .SourceSpec }} --frontend-target antd-v5 --check
{{- end }}
{{- if .HasFrontendV6 }}
go run ./cmd/mss module generate {{ .SourceSpec }} --frontend-target antd-v6 --check
{{- end }}
{{- if and (not .HasFrontendV5) (not .HasFrontendV6) }}
go run ./cmd/mss module generate {{ .SourceSpec }} --check
{{- end }}
cd admin && GOWORK=off go test ./modules/{{ .ModuleName }}/...
go run ./cmd/mss verify --module {{ .ModuleName }}
```

Backend authorization is mandatory even when the frontend hides an action.
Generated routes are inert until a composition root supplies a non-nil backend authorizer.
