// Code generated by mss from {{ .SourceSpec }}. DO NOT EDIT.

import { request } from '@umijs/max';

import type { PageResult, {{ .ModelName }}, {{ .ModelName }}Query } from './types';

const resourcePath = '/admin/api/{{ .APIResource }}';

export async function list{{ .ModelName }}(
  params: {{ .ModelName }}Query,
  options?: Record<string, unknown>,
) {
  return request<PageResult<{{ .ModelName }}>>(resourcePath, {
    method: 'GET',
    params,
    ...(options || {}),
  });
}

export async function get{{ .ModelName }}(id: string, options?: Record<string, unknown>) {
  return request<{{ .ModelName }}>(`${resourcePath}/${id}`, {
    method: 'GET',
    ...(options || {}),
  });
}

export async function create{{ .ModelName }}(
  body: Partial<{{ .ModelName }}>,
  options?: Record<string, unknown>,
) {
  return request<{{ .ModelName }}>(resourcePath, {
    method: 'POST',
    data: body,
    ...(options || {}),
  });
}

export async function update{{ .ModelName }}(
  id: string,
  body: Partial<{{ .ModelName }}>,
  options?: Record<string, unknown>,
) {
  return request<{{ .ModelName }}>(`${resourcePath}/${id}`, {
    method: 'PUT',
    data: body,
    ...(options || {}),
  });
}

export async function delete{{ .ModelName }}(id: string, options?: Record<string, unknown>) {
  return request<void>(`${resourcePath}/${id}`, {
    method: 'DELETE',
    ...(options || {}),
  });
}
